<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Unirse al Juego</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="flex items-center justify-center h-screen bg-gray-100">
    <div class="bg-white p-8 rounded-lg shadow-lg max-w-md w-full">
        <!-- Lista de Jugadores -->
        <div class="mt-6">
            <h2 class="text-lg font-bold text-gray-700 mb-4">Espera a que todos se unan a la partida:</h2>
            <div class="bg-gray-100 p-4 rounded-lg h-32 overflow-y-auto">
                <% if (Object.keys(jugadores).length > 0) { %>
                    <% Object.keys(jugadores).forEach(nombre => { %>
                        <div class="text-gray-700 font-semibold mb-2"><%= nombre %></div>
                    <% }) %>
                <% } else { %>
                    <p class="text-gray-500">No hay jugadores aún.</p>
                <% } %>
            </div>
        </div>
    </div>
    <script>
        setInterval(() => {
          window.location.href = '/join_jugadores';
        }, 6000); // 3000 milisegundos = 3 segundos
      </script>      
</body>
</html>

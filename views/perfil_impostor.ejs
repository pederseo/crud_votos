<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Responder al Juego</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="flex items-center justify-center h-screen bg-gray-100">

    <div class="bg-white p-8 rounded-lg shadow-lg max-w-md w-full">
        <h1 class="text-2xl font-bold mb-6 text-center">Perfil de jugador</h1>

        <!-- Información recibida del backend -->
        <div class="space-y-4">
            <div>
                <h1 class="px-4 py-2 border rounded-lg bg-gray-100">Nombre: <%= nombre %></h1>
            </div>
            
            <div>
                <h1 class="px-4 py-2 border rounded-lg bg-gray-100">Rol: <%= roll %></h1>
            </div>
        </div>

        <!-- Formulario de respuesta -->
        <form action="/enviar_respuesta" method="POST" class="space-y-4 mt-6">
            <div>
                <label for="respuesta" class="block text-gray-700 font-bold mb-2">Escribe tu respuesta:</label>
                <input type="text" id="respuesta" name="respuesta" required 
                    class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <!-- Botón Enviar -->
            <div class="text-center">
                <button type="submit" 
                    class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                    Enviar
                </button>
            </div>
        </form>
    </div>

</body>
</html>
